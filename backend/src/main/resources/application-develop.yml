spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/unipocket?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: genesis
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    root: info